# PRD — "10년 뒤의 나" 그림 생성 웹앱

> 이 문서는 AI(또는 개발자)에게 구현을 맡기기 위한 제품 요구사항 문서입니다. 제품 결정은 모두 확정되어 있습니다(§10). 문서 마지막 "구현자에게" 섹션을 먼저 읽어 주세요.

---

## 1. 개요

- **한 줄 요약**: 중학생이 스마트폰으로 접속해, 정해진 입력 폼을 채우면 OpenAI 이미지 API가 "10년 뒤 가장 행복한 어느 하루의 구체적인 한 장면"을 그려주고, 반 전체가 익명 갤러리로 함께 감상하는 웹앱.
- **사용 상황**: 교사가 강의(수업) 중 세션을 열고, 학생 약 20명이 각자 폰으로 동시에 사용. 1회 강의 = 1개 세션.
- **핵심 가치**: 설치 없음, 로그인 없음, 3분 안에 결과. 디자인은 깔끔하고 UI는 최대한 단순.

## 2. 사용자

| 역할 | 설명 | 기기 |
|---|---|---|
| 학생 (주 사용자) | 중학생 약 20명. 계정·앱 설치 불가 전제. | 스마트폰 브라우저 |
| 교사 (관리자) | 세션 개설, 진행 상황 확인, 부적절한 그림 숨김, 갤러리를 프로젝터에 띄움 | 노트북 브라우저 |

## 3. 목표 / 비목표

**목표**
- 학생 1인당 그림 생성 정확히 최대 2회.
- 20명 동시 요청에도 오류 없이 처리(대기 시간은 허용, 실패는 불허).
- 갤러리는 완전 익명(그림 + 입력 키워드만 노출).
- 모바일 세로 화면에서 스크롤·탭 최소화.

**비목표 (이번 버전에서 하지 않음)**
- 학생 얼굴 사진 업로드/닮은 얼굴 생성 (미성년자 개인정보 이슈로 배제. 텍스트 입력만 사용)
- 회원가입·로그인, 소셜 공유, 댓글/좋아요
- 다국어 (한국어만)

## 4. 사용자 흐름

### 4-1. 교사
1. `/teacher` 접속 → 관리자 비밀번호(환경변수) 입력.
2. "새 세션 만들기" → 세션 코드(예: `A7K2`, 4자리) + QR 코드 자동 생성.
3. 세션 설정: 1인당 횟수(기본 2), 세션 총 생성 상한(기본 50), 사용 가능 스타일.
4. QR/코드를 화면에 띄움. 진행 현황(생성 수, 대기 수)이 실시간 표시.
5. 갤러리 화면(`/g/A7K2`)을 프로젝터에 띄워 함께 감상. 부적절한 그림은 교사 화면에서 "숨김".
6. 수업 종료 → "세션 닫기"(더 이상 생성 불가, 갤러리는 열람 가능).

### 4-2. 학생
1. QR 스캔 또는 URL + 세션 코드 입력 → 세션 입장. **이름 입력 없음.**
2. 입력 폼(5장 이내, 아래 §5-1) 작성 → "그림 그리기" 버튼.
3. 대기 화면: "그림을 그리고 있어요… 앞에 N명" + 진행 애니메이션 (예상 20~60초).
4. 결과 화면: 그림 크게 보기, 저장(다운로드), "갤러리에 올리기"(기본 ON) / "다시 그리기(남은 1회)".
5. 갤러리 화면에서 반 친구들 그림 감상.

## 5. 기능 요구사항

### F1. 그림 생성 (OpenAI 이미지 API)

**핵심 원칙**: 결과물은 "직업 초상화"가 아니라 **"10년 뒤 가장 행복한 어느 하루의 구체적인 한 장면"** 이다. 직업은 입력 항목이 아니며, 학생이 1번 답변에서 자연스럽게 언급할 수 있을 뿐이다. 인물은 장면의 주인공이지만 화면의 약 1/3만 차지하고, 장소·행동·주변 사물이 함께 읽히는 중간~와이드 샷이어야 한다.

**5-1. 입력 폼 (장면 묘사 구조, 스텝 5개)**
한 화면에 한 질문씩, 진행 표시 점 5개. 1번만 자유 문장이고 나머지는 칩 선택 위주로 품질을 보장한다.

| # | 질문 (학생에게 보이는 문구) | 입력 방식 | 필수 |
|---|---|---|---|
| 1 | 10년 뒤, 가장 행복한 어느 하루. 나는 지금 **무엇을 하고 있나요?** | 자유 문장 1개(최대 40자, 플레이스홀더: "예) 내가 만든 게임을 친구들과 처음 같이 해보고 있어요") + 시작 도우미 칩 3개(누르면 입력창에 예문 채워짐, 수정 가능): "무대 위에서 노래를 부르고 있어요" / "내가 만든 가게에서 손님을 맞아요" / "반려견과 바닷가를 달리고 있어요" | ✔ |
| 2 | **어디**인가요? 주변에 무엇이 보이나요? | 장소 칩 단일 선택(도시, 바다, 숲/산, 우리 집, 무대, 연구실, 우주, 카페, 운동장, 공방/스튜디오, 기타…) + "보이는 것" 자유 입력 1개(최대 15자, 선택. 예: 큰 창문, 트로피, 망원경) | ✔ (장소만) |
| 3 | 그 순간 나는 **누구와** 있나요? | 칩 다중 선택 최대 2개(혼자, 친구들, 가족, 동료들, 반려동물, 팬/관객, 학생들, 아이) | ✔ |
| 4 | 그 장면의 **분위기와 시간**은? | 분위기 칩 단일(설레는, 평화로운, 당당한, 따뜻한, 신나는, 몽환적인) + 시간 칩 단일(아침, 한낮, 노을, 밤) | ✔ |
| 5 | 그림 스타일 | 카드 3개 중 선택: **애니메이션 / 실사(영화 스틸) / 수채화** — 각 카드에 예시 썸네일(교사 화면에서 생성·고정, §F4) | ✔ |
| + | 성별 표현(선택, 5번 화면 하단) | 남 / 여 / 표시 안 함(기본) — "표시 안 함"이면 프롬프트에 성별 언급 없음 | 선택 |

- 1번 자유 문장은 서버에서 (a) 금칙어 필터 (b) OpenAI Moderation API 통과 후 사용. 거절 시 "1번 문장을 조금 바꿔볼까요?" 안내, 횟수 미차감.
- 1번 문장은 한국어 그대로 프롬프트에 넣지 않는다. 서버가 저비용 텍스트 모델(예: gpt-4o-mini 급)로 **영어 한 구절로 번역·정제**(행동 중심, 20단어 이내, 실존 인물·브랜드 제거)한 뒤 템플릿에 삽입한다. 번역 실패 시 원문 그대로 삽입(모델이 한국어도 이해함).

**5-2. 프롬프트 엔지니어링 (서버에서 조립, 학생에게 노출 안 함)**
- 템플릿 골격(구현 시 조정 가능):
  ```
  {style_descriptor}. A cinematic medium-wide scene, vertical 3:4.
  A Korean young adult in their mid-20s{gender_phrase} is {action_en} — this action is the heart of the image.
  Setting: {place_en}{visible_detail_phrase}, at {time_of_day}. {companions_phrase}
  The environment, the activity, and the surrounding objects are as important as the person;
  the person occupies roughly one third of the frame and is clearly engaged in the action, not posing.
  The moment feels {mood_en}. Rich environmental detail, natural {time_lighting} lighting, high detail.
  No text, no captions, no watermark, no logos, no split panels.
  ```
- 변수 사전 예시: `time_of_day/time_lighting` — 아침: `early morning / soft golden`, 한낮: `midday / bright`, 노을: `sunset / warm orange`, 밤: `night / cool ambient and city-light`. `companions_phrase` — 혼자: `The person is alone, enjoying the moment.` / 친구들: `A few friends around them share the moment.` 등.
- 스타일별 `style_descriptor` 사전:
  - 애니메이션: `Hand-drawn anime illustration in the style of a heartfelt animated film, soft cel shading, warm colors, detailed background art`
  - 실사: `Photorealistic cinematic film still, 35mm look, natural depth of field, realistic textures`
  - 수채화: `Delicate watercolor painting, visible paper texture, gentle washes of color, loose expressive brushwork`
- 금칙어: 실존 인물·유명인·브랜드/로고·폭력·무기·선정성·자해 관련 키워드. 자유 입력 두 곳(1번 문장, 2번 보이는 것)에 모두 적용.
- **모델 확정**: `gpt-image-2`, quality `medium`, size `1024x1536`(세로 3:4). 장당 약 $0.041, 40장 기준 약 $1.6. 교사 설정으로 `high`(장당 약 $0.165) 전환 가능. 프롬프트 튜닝·부하 테스트는 `gpt-image-1-mini` / `low`(장당 약 $0.005)로 수행하도록 환경변수 `IMAGE_MODEL`, `IMAGE_QUALITY` 분리.
- 참고: `gpt-image-1`은 2026-10-23 폐기 예정이므로 사용하지 않는다. DALL·E 계열은 API에서 제거됨.

**5-3. 실패 처리**
- API 오류·모더레이션 거절 시 횟수를 **차감하지 않음**. 학생에게는 "다른 단어로 다시 시도해 볼까요?"라는 쉬운 문구.
- 모더레이션 거절 시 어떤 입력이 문제였는지 대략 안내(예: "4번 답변을 바꿔보세요").

### F2. 익명 갤러리
- 경로 `/g/{세션코드}`. 세션 코드 아는 사람만 열람.
- 카드: 그림 + 태그 형태의 키워드(장소·함께 있는 사람·분위기·시간·스타일)만. 1번 자유 문장은 노출하지 않음(개인 식별 가능성). 이름·기기 ID·시간 노출 없음.
- 정렬: 최신순. 새 그림은 새로고침 없이 나타남(폴링 5초 또는 실시간 구독).
- 카드 탭 → 전체 화면 보기(좌우 스와이프로 다음/이전).
- 프로젝터 모드(교사용 토글): 큰 그리드, 자동 슬라이드쇼(장당 6초), 다크 배경.
- 학생은 본인 그림만 갤러리에서 내릴 수 있음(기기 ID로 본인 확인).
- 교사 숨김 처리된 그림은 갤러리에서 즉시 사라짐.

### F3. 1인당 2회 제한 (기기 기반 익명 ID)
- 최초 접속 시 브라우저에 UUID 생성 → `localStorage` + 쿠키(1년) 이중 저장. 서버는 `(세션코드, 기기ID)` 조합으로 생성 횟수 카운트.
- 서버가 횟수를 판정하며, 클라이언트 값은 신뢰하지 않음.
- 알려진 한계(수용): 브라우저 데이터 삭제·시크릿 모드·다른 폰 사용 시 초기화됨. 교실 환경에서 큰 문제는 아니며, 아래 백스톱으로 비용 폭주만 막음.
- 백스톱: 세션 총 생성 상한(기본 50) + 세션당 분당 생성 상한(예: 15) + 세션 닫기.
- 교사 화면에 기기별 생성 횟수가 익명 ID로 보이며, 필요 시 "이 기기 횟수 초기화"(폰 문제 등) 가능.

### F4. 교사 관리 화면
- 세션 생성/닫기/삭제, 설정 변경(횟수·상한·스타일·동시 실행 수), 실시간 현황(생성 완료 수 / 대기 수 / 실패 수 / 예상 비용).
- 한 번에 활성 세션은 1개만(새 세션을 만들면 이전 세션은 자동으로 닫힘). 지난 세션은 목록에서 갤러리 열람·ZIP 다운로드·삭제만 가능.
- "스타일 예시 생성" 버튼: 스타일당 예시 1장을 생성해 학생 폼의 스타일 카드 썸네일로 고정(1회 실행, 재생성 가능).
- 그림 목록(익명 ID 포함) + 숨김/복구 버튼.
- 세션 전체 그림 ZIP 다운로드.
- 접근은 단일 관리자 비밀번호(환경변수). 회원 시스템 불필요.

## 6. 기술 아키텍처

### 6-1. 스택 (제안, 대체 가능)
- 프론트: **Next.js (App Router) + TypeScript + Tailwind**. 모바일 퍼스트. PWA 매니페스트 포함(홈 화면 추가 가능, 오프라인은 불필요).
- 백엔드: Next.js Route Handlers(서버리스). **OpenAI API 키는 서버 환경변수에만 존재.** 클라이언트는 절대 OpenAI를 직접 호출하지 않음.
- DB: Postgres(Supabase 또는 Vercel Postgres). 테이블: `sessions`, `devices`, `jobs`, `images`.
- 이미지 저장: 오브젝트 스토리지(Supabase Storage 또는 Vercel Blob). OpenAI 응답(base64/URL)을 즉시 스토리지에 저장하고 공개 URL을 DB에 기록. (OpenAI 임시 URL은 만료되므로 반드시 재저장)
- 배포: Vercel.

### 6-2. 동시성 설계 — "20명이 동시에 눌러도 되는가"에 대한 답
OpenAI 요금 제한은 API 키가 아니라 **계정(조직) 단위의 분당 요청 수(RPM/IPM)** 이며, 20명 동시 사용은 다음 설계로 안전하게 처리한다.

1. **비동기 잡 큐**: 학생이 "그리기"를 누르면 서버는 즉시 `job_id`를 반환(상태 `queued`), OpenAI 호출은 백그라운드 워커가 처리. 클라이언트는 `/api/jobs/{id}`를 2~3초 간격 폴링(또는 SSE)해 `queued → running → done/failed` 상태와 대기 순번을 표시.
2. **동시 실행 수 제한**: 워커가 동시에 OpenAI로 보내는 요청을 N개(**기본 3**, 환경변수 `OPENAI_CONCURRENCY`)로 제한. 나머지는 큐에서 순서대로 대기.
3. **자동 재시도**: 429/5xx 응답 시 지수 백오프(2s → 4s → 8s, 최대 3회)로 재시도. `Retry-After` 헤더 존중. 3회 실패 시 `failed` 처리 + 횟수 미차감.
4. **서버리스 타임아웃 대응**: 이미지 생성이 10~60초 걸리므로 요청-응답 한 번에 처리하지 않는다. 옵션 A) Vercel 함수 `maxDuration` 상향 + `waitUntil`/백그라운드 함수, 옵션 B) 외부 큐(Inngest, Trigger.dev, QStash) 사용, 옵션 C) Supabase Edge Function. 구현자가 택하되, **한 요청이 10초 제한에 걸려 실패하는 구조는 금지.**
5. **용량 산정**: 20명 × 2회 = 40장. 장당 약 20초, 동시 3개 처리 시 전체 소요 약 4~5분. 마지막 학생 최대 대기 약 3~4분 → 대기 화면 UX로 흡수.
6. **비용 가드**: 세션 총 상한·분당 상한(§F3)을 서버가 강제. 예상 비용을 교사 화면에 표시.
7. **수업 전 점검 절차(필수 산출물)**: `scripts/load-test.ts` — 지정 세션에 20건을 동시에 투입하고 429 발생 횟수·평균 소요 시간을 출력. 429가 0이면 교사 화면에서 동시 실행 수를 5로 올려 재시험, 429가 나오면 3 유지. 티어 사전 확인 없이도 이 절차만으로 안전하게 운영 가능하도록 한다.

### 6-3. 데이터 모델 (요약)
- `sessions(code, status, per_device_limit, total_limit, allowed_styles, created_at, closed_at)`
- `devices(id uuid, session_code, generation_count, created_at)`
- `jobs(id, session_code, device_id, inputs jsonb, prompt, status, attempts, error, created_at, finished_at)`
- `images(id, job_id, session_code, device_id, url, tags jsonb, is_hidden, in_gallery, created_at)`

### 6-4. 안전·개인정보
- 이름·얼굴·연락처·직업 등 개인정보 수집 없음. 기기 ID는 랜덤 UUID.
- 학생 자유 텍스트(1번 문장, 2번 보이는 것)는 금칙어 필터 + OpenAI Moderation API 이중 검사. 1번 문장은 갤러리에 노출하지 않음. 생성 결과도 안전 기준 위반 시(API 거절) 재입력 유도.
- 갤러리는 세션 코드 없이는 접근 불가, 검색 엔진 색인 차단(`noindex`).
- 데이터 보존: **자동 삭제 없음.** 교사가 세션 단위로 "세션 삭제"(그림·잡·기기 기록 일괄 삭제, 2단계 확인)할 때까지 보존. 삭제 전 ZIP 다운로드 안내.

## 7. UI / UX 요구사항

- **모바일 퍼스트**: 360px 폭 기준 설계, 세로 방향. 모든 주요 버튼은 화면 하단 고정, 높이 48px 이상, 한 손 조작.
- **스텝 폼**: 한 화면 한 질문. 칩은 큼직하게(터치 영역 44px+). 뒤로가기 가능. 진행 점 표시.
- **디자인 톤**: 미니멀, 여백 넉넉, 한 가지 포인트 색 + 중립 회색. 장식 요소 최소. 시스템 폰트(Pretendard 권장).
- **로딩**: 스켈레톤·진행 애니메이션·대기 순번 표시. "잘 그려지고 있어요" 류의 짧은 격려 문구 로테이션.
- **결과 화면**: 그림이 화면의 70% 이상. 저장 버튼(이미지 다운로드), 갤러리 올리기 토글, 다시 그리기(남은 횟수 표시).
- **접근성**: 색 대비 WCAG AA, 칩 선택 상태는 색 + 체크 아이콘으로 이중 표시.
- **데스크톱(교사)**: 반응형으로 자연스럽게 확장. 프로젝터 모드는 다크 배경 + 큰 그리드.
- **문구**: 모두 한국어, 중학생 눈높이의 짧은 문장. 에러 문구에 기술 용어 금지.

## 8. 비기능 요구사항
- 초기 로딩 3초 이내(3G 기준), 이미지 lazy-load, 썸네일(예: 512px) 별도 생성.
- 폴링 간격 2~3초, 갤러리 갱신 5초.
- 20명 동시 접속 시 프론트/백엔드 오류율 0%. OpenAI 재시도 포함 최종 실패율 <2%.
- 브라우저: iOS Safari 최신 2버전, Android Chrome 최신 2버전.

## 9. 수용 기준 (Acceptance Criteria)
- [ ] 학생이 이름 입력 없이 QR로 입장해 5스텝 폼을 완료하고 60초 내(대기 제외) 그림을 받는다.
- [ ] 같은 브라우저에서 3번째 생성 시도는 서버가 거부하고, 실패한 생성은 횟수를 차감하지 않는다.
- [ ] 20개 요청을 동시에 보내는 부하 테스트에서 전부 `done` 또는 명확한 `failed`로 종료되고, 대기 순번이 화면에 표시된다.
- [ ] 갤러리에 이름·ID·시간이 노출되지 않고, 세션 코드 없이는 접근할 수 없다.
- [ ] 교사가 그림을 숨기면 5초 이내 갤러리에서 사라진다.
- [ ] OpenAI API 키가 클라이언트 번들·네트워크 응답 어디에도 나타나지 않는다.
- [ ] 세션 총 상한 도달 시 모든 학생에게 "오늘 그림은 모두 그렸어요" 안내가 뜬다.

## 10. 확정된 결정 사항
| 항목 | 결정 |
|---|---|
| 1인당 제한 방식 | 기기(브라우저) 기반 익명 UUID, 서버 판정 |
| 갤러리 익명성 | 완전 익명 — 그림 + 키워드 태그만 |
| 그림 콘셉트 | 직업 초상화가 아닌 "행복한 하루의 구체적 장면", 인물은 프레임의 약 1/3 |
| 스타일 | 애니메이션 / 실사 / 수채화 3종, 학생 선택 |
| 이미지 모델 | gpt-image-2 / medium / 1024×1536 (테스트는 gpt-image-1-mini low) |
| 이미지 비율 | 세로 3:4 |
| 동시 운영 학급 | 1개 (활성 세션 1개, 지난 세션은 열람·삭제만) |
| 데이터 보존 | 자동 삭제 없음, 교사가 세션 삭제할 때까지 보존 |
| 학생 다운로드 | 허용 |
| 얼굴 사진 업로드 | 하지 않음 |
| OpenAI 동시 실행 수 | 기본 3, 부하 테스트 후 교사 화면에서 조정 |
| 스타일 예시 썸네일 | 교사 화면 "예시 생성" 버튼으로 배포 후 생성·고정 |

## 11. 구현자에게
- 제품 결정은 §10에서 모두 확정되었습니다. 남은 기술 선택은 §6-2의 큐/백그라운드 실행 방식 하나이니, 선택 근거와 함께 알려주세요.
- 1차 산출물 순서 제안: (1) 데이터 모델 + 생성 API + 큐/재시도 → (2) 학생 폼·대기·결과 화면 → (3) 갤러리 → (4) 교사 화면(예시 생성 버튼 포함) → (5) 부하 테스트 스크립트.
- 코드에 하드코딩된 값(횟수, 상한, 동시 실행 수, 모델명, 폴링 간격)은 모두 환경변수 또는 세션 설정으로 빼주세요.
